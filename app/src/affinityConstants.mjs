export const CHARACTER_AFFINITY_INITIAL_VALUE = 25;
// A homunculus starts already devoted to its creator (born in the atelier, aware of who lit it), so its
// affinity opens far above the academy 25. Same 0..100 bounds; only the initial value differs.
export const HOMUNCULUS_AFFINITY_INITIAL_VALUE = 50;
export const CHARACTER_AFFINITY_MIN = 0;
export const CHARACTER_AFFINITY_MAX = 100;
export const BUDDY_AFFINITY_DELTA = 10;
export const ENEMY_AFFINITY_DELTA = -10;
